def schedule_appointment(user_id, datetime_str):
    """Placeholder scheduler."""
    print(f"Scheduling appointment for {user_id} at {datetime_str}")
    return {"status": "scheduled", "when": datetime_str}
